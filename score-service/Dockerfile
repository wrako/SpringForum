# Базовый образ, использующий OpenJDK 17 для запуска Spring Boot приложения
FROM openjdk:17-jdk-slim

# Определение рабочей директории в контейнере
WORKDIR /app

# Копируем сгенерированный JAR файл в рабочую директорию контейнера
COPY target/*.jar score-service.jar

# Экспонируем порт, который будет использовать ваше приложение (например, 8080)
EXPOSE 8086

# Определяем команду для запуска приложения
ENTRYPOINT ["java", "-jar", "score-service.jar"]
